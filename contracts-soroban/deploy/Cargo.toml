[package]
name = "deploy-membership-nft"
version = "1.0.0"
edition = "2021"
rust-version = "1.79"
publish = false

[[bin]]
name = "deploy"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", default-features = false, features = ["std", "derive", "help", "usage", "error-context"] }

[workspace]
members = []
