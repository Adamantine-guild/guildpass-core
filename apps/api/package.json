{
  "name": "@guildpass/api",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@guildpass/shared-types": "workspace:*",
    "fastify": "^4.29.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
